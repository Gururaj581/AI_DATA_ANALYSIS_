import pandas as pd

# Load the existing CSV file
try:
    df = pd.read_csv('data.csv')
    print("CSV file loaded successfully!")
    
    # Adding new columns with sample data
    df['NewColumn1'] = ['A', 'B', 'C', 'D', 'E']  # Replace with your data
    df['NewColumn2'] = [1, 2, 3, 4, 5]            # Replace with your data
    
    # Save the updated DataFrame back to the CSV
    df.to_csv('data.csv', index=False)  # index=False prevents saving the index column
    
    print("\nColumns added and CSV file updated successfully!")
    print("\nFirst 5 rows of the updated DataFrame:")
    print(df.head())

except FileNotFoundError:
    print("Error: File not found. Please check the file path.")
except Exception as e:
    print(f"An error occurred: {str(e)}")
